package main.com.teamalfa.blindvirologists.turn_handler;

/**
 * Interface that every steppable implements.
 */
public interface Steppable {
    /**
     * A method that they do for every Tick.
     */
    void step();
}
